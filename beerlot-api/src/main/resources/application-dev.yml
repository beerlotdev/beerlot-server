spring:
  config:
    import:
      - secret/dev/application-cloud-dev.yml
      - secret/dev/application-oauth-dev.yml

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL81Dialect
